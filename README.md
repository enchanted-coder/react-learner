# To publish this site:

- ### npm run build on local machine
```
    npm run build
```

- ### git subtree push --prefix dist origin gh-pages
```
    git subtree push --prefix dist origin gh-pages
```

